import { ICommand } from '@nestjs/cqrs'

export class GetCronJobsQuery implements ICommand {
  constructor /*     public readonly filter: {
      name?: string
      schedule?: string
      task?: string
      isActive?: boolean
    } = {}, */() {}
}
